import React, { Component } from "react";
import PropTypes from "prop-types";
import "../styles/CompanyLeftMenu.css";

export default class CompanyLeftMenu extends Component {
	static propTypes = {
		history: PropTypes.object
	};

	state = {};

	render() {
		return <div />;
	}
}
